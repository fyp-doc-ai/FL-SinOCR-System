# Phase 3: Communication-efficient training -- encoder-only updates
# Inherits from base_config.yaml

fl:
  algorithm: "fedavg"
  num_rounds: 50
  clients_per_round: 3

peft:
  method: "encoder_only"

training:
  local_epochs: 3
  learning_rate: 5.0e-5

logging:
  experiment_name: "encoder_only"
